/**
 * The light behind the home page hero: a soft glow that follows the pointer
 * (HomeEffects sets --hx, --hy and --hero-lit). Decoration only.
 */
export function HeroLight() {
  return <div className="hero-light" aria-hidden />;
}
